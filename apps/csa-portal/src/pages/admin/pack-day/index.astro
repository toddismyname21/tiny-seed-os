---
/**
 * /admin/pack-day — redirects to the upcoming cycle's pack-day dashboard.
 *
 * Pack day is THE first thing Todd opens, so we skip the week-picker
 * landing and bounce straight to /admin/pack-day/<upcoming-mon>.
 */
import { currentDeliveryWeek } from '../../../lib/cycle';
return Astro.redirect(`/admin/pack-day/${currentDeliveryWeek()}`, 303);
---

